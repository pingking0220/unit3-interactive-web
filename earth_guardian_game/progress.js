const STORAGE_KEY='earthGuardianProgressV1';function defaultProgress(){const mastery={};ZONES.forEach(z=>mastery[z.code]=0);return{mastery,wrongQuestions:[],unlockedCards:[],energy:0,answered:{}};}function loadProgress(){try{const s=localStorage.getItem(STORAGE_KEY);if(!s)return defaultProgress();const p=JSON.parse(s),b=defaultProgress();return{...b,...p,mastery:{...b.mastery,...(p.mastery||{})},wrongQuestions:p.wrongQuestions||[],unlockedCards:p.unlockedCards||[],answered:p.answered||{}}}catch(e){return defaultProgress()}}function saveProgress(p){localStorage.setItem(STORAGE_KEY,JSON.stringify(p))}function resetProgressData(){localStorage.removeItem(STORAGE_KEY)}function getMastery(z){return Math.max(0,Math.min(100,progress.mastery[z]||0))}function addMastery(z,a){progress.mastery[z]=Math.max(0,Math.min(100,(progress.mastery[z]||0)+a))}function unlockCard(n){if(n&&!progress.unlockedCards.includes(n)){progress.unlockedCards.push(n);return true}return false}function addWrongQuestion(id){if(!progress.wrongQuestions.includes(id))progress.wrongQuestions.push(id)}function removeWrongQuestion(id){progress.wrongQuestions=progress.wrongQuestions.filter(x=>x!==id)}function isZoneUnlocked(z){return true}
